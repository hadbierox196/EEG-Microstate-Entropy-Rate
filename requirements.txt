mne>=1.0
pycrostates>=0.6
scikit-learn>=1.0
scipy>=1.7
numpy>=1.21
pandas>=1.3
matplotlib>=3.5
seaborn>=0.11
tqdm>=4.62

# Optional, only needed for the initial dataset download step.
# Install one of these separately if 'aws' is not already on your PATH:
#   pip install awscli
#   pip install openneuro-py
